import styled from "styled-components";
import { colors } from "../../constants/theme";

export const StyledForm = styled.form`
  /* background-color: ${colors.neutral.magnolia}; //Cor temporária  */
  padding: 24px;
  margin: auto;
  max-width: 90%;
  border-radius: 10px;
`;
